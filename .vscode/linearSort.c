#include <stdio.h>
#include <conio.h>
#define MAX 10

int main(){
    int arr[MAX],n, i,j;
    printf("Enter the array size : ");
    scanf("%d", &n);

    for(i = 0; i < n; i++){
        printf("Enter the element %d : ", (i+1));
        scanf("%d",&arr[i]);
    }
    // Sorting the array
    for (i = 0; i < n; i++)
    {
        for ( j = (i+1); j < n; j++)
        {
            
        }
        
    }
    
}